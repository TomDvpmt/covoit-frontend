import { Box } from "@mui/material";

const Footer = () => {
    return (
        <Box
            component="footer"
            sx={{ mt: "3rem", bgcolor: "#f5f5f5", minHeight: "100px" }}>
            Footer
        </Box>
    );
};

export default Footer;
